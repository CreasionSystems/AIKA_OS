{
  "name": "aika-os",
  "version": "0.0.0",
  "private": true,
  "description": "AI統合ワークステーション (Electron + React + TypeScript)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
